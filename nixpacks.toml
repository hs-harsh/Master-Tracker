[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "cd server && npm install",
  "cd client && npm install"
]

[phases.build]
dependsOn = ["install"]
cmds = ["cd client && npm run build"]

[start]
cmd = "npm run start"
